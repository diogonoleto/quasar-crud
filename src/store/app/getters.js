export const dialogProfile = ({ dialogProfile }) => dialogProfile
export const dialog = ({ dialog }) => dialog
export const header = ({ header }) => header
export const menuUser = ({ menu }) => menu.user
export const title = ({ title }) => title

export const current = ({ current }) => current
export const addresses = ({ addresses }) => addresses
